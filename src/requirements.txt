# Python side of the toolchain. Versions as pinned in ../resources.md; each
# pin was checked to exist on PyPI. Newer releases exist for the first two
# (cadquery 2.8.0, trimesh 5.0.0) — bump deliberately, not by accident.
#
# The FEA solver, CalculiX (ccx), is NOT here: it is not on PyPI.
# Install it with fetch/install_calculix.sh.

# geometry: the artifact the agent writes
cadquery==2.5.2

# mesh work: clearance checks, decimation, cropping, watertightness
trimesh==4.12.2

# tet meshing for FEA, and format bridging
gmsh==4.15.2
meshio==5.3.5

# read solver results back out
ccx2paraview==3.2.0

# reading the shape model .mat files from Python (no MATLAB needed)
numpy>=1.24
scipy>=1.10

# --- CT / mask path only (see refs/DATA_NOTES.md before relying on it) ---
# nibabel>=5.4          # read the NIfTI mask volumes
# scikit-image>=0.24    # marching cubes, mask -> surface
# TotalSegmentator>=2.18  # CT -> masks; check the WEIGHTS licence separately
